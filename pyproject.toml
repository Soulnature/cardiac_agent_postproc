[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cardiac-agent-postproc"
version = "0.1.0"
description = "Multi-agent, no-GT reward optimization + GT evaluation for cardiac MRI segmentation post-processing"
requires-python = ">=3.9"
