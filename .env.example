# Optional: only needed if you want RevisionController to call an LLM to propose parameter changes.
# OpenAI-compatible API (works with OpenAI, Azure OpenAI, or local Ollama/Qwen OpenAI-compatible server)
LLM_ENABLED=0

OPENAI_BASE_URL=http://localhost:11434/v1
OPENAI_API_KEY=ollama
OPENAI_MODEL=qwen2.5:latest

# If you want to pin device hints etc.
# CUDA_VISIBLE_DEVICES=0
